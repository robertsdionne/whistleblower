// new module script

String news_updates[];
int news_updates_length = 3;
int news_updates_index = 0;

int last_update_time = 0;
int update_interval = 10;

function game_start() {
  news_updates = new String[news_updates_length];
  news_updates[0] = "One";
  news_updates[1] = "Two";
  news_updates[2] = "Three";
  DateTime *now = DateTime.Now;
  last_update_time = now.RawTime;
}

function repeatedly_execute() {
//  DateTime *now = DateTime.Now;
//  if (news_updates_index < news_updates_length) {
//    if (last_update_time + update_interval < now.RawTime) {
//      DisplayAt(100,  100,  200, news_updates[news_updates_index]);
//      news_updates_index += 1;
//      last_update_time = now.RawTime;
//    }
//  }
}
